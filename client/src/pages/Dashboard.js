import Header from "../components/Header"

export default function Dashboard() {
    return (
        <div className="header-container">
            < Header/>
            <form>
                <h1>Dashboard</h1>
            </form>
        </div>
    )
}